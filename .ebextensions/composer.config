commands:
  01setupComposer
    command: curl -s https://getcomposer.org/installer | php && mv composer.phar /usr/bin/composer && composer self-update

option_settings:
  - namespace: aws:elasticbeanstalk:application:environment
    option_name: COMPOSER_HOME
    value: /root